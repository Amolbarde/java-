# LIST - list interface is a part of the java collection freamwork and represent an ordered collection of element.

- you can access element by their index, add duplicstes and maintain the insertion order 

- LIST = Ordered + index based
- like a notebook 
  . item stay in order.
  . Duplicate values allowed.
  . Access using index.
  
---------------------------------------------------

# LIST
  -ArrayList - Fast searching
  -LinkedList - fast insertion/deletion
  -Vector     - old synchronized Arraylist
  
-----------------------------------------------------

# List Method
 1) add() - add element
 2) get() -  	Returns the element at the specified position
 3) set() - Replaces the element at the specified position
 4) remove() - Removes the element at the specified position
 5) size() - Returns the number of elements in the list
